# VRCX 改版维护指南（写给完全不了解原理的你）

这份文档不教你背命令，而是先讲清楚"东西是怎么跑起来的"，然后你会发现每一步该干什么是很自然的。

全文里出现的每个术语，第一次出现时都会解释。看不懂的段落可以跳到最后的《名词对照表》。

---

## 第一部分：你的 VRCX 是由什么组成的

### 1.1 三层结构

```
VRChat 官方服务器
      │  （网络请求：好友列表、玩家资料、动态…）
      ▼
┌─────────────────────────────────────────────┐
│  E:\VRCX\VRCX.exe   ← .NET 写的"宿主"程序      │
│      负责：登录、发网络请求、读写数据库、开窗口   │
│                                               │
│  E:\VRCX\html\      ← 你看到的界面（前端）      │
│      负责：把数据显示成窗口里的样子、响应点击     │
└─────────────────────────────────────────────┘
      │
      ▼
C:\Users\28041\AppData\Roaming\VRCX\VRCX.sqlite3
      ← 数据库，记录你看到过的所有历史
```

关键认知：**你看到的界面，和干活的程序，是两个独立的东西。**

VRCX.exe 是一个 .NET 程序，它开了一个浏览器内核（CefSharp，相当于内置了一个 Chrome），然后把 `E:\VRCX\html\` 里的网页加载进这个窗口显示给你看。所以：

- `E:\VRCX\html\` 里的东西本质上就是**一堆网页文件**（HTML + JavaScript + CSS + 图片）
- 换掉这个文件夹，就等于换了界面，而 VRCX.exe 完全不知道也不关心
- 这就是为什么我能把你的功能"装进"官方 VRCX —— 我只换了 html 文件夹

数据库 `VRCX.sqlite3` 是另一个独立的东西。它是 VRCX.exe 在用，我改前端从来不碰它。你的登录状态、好友记录、简介历史全在里面，**只要不删它，历史就一直在**。

### 1.2 为什么"改了代码"不能直接生效

网页文件（`src/` 里的 `.vue`、`.js`）浏览器是不能直接运行的——它们用了浏览器的 Chrome 不支持的写法（模块导入、Vue 模板语法、TypeScript 注释等）。

所以需要一步"**构建**"（build）：用一个工具（Vite）把 `src/` 里几百个源文件翻译、压缩、合并成浏览器能直接跑的 `build/html/`。

```
C:\Users\28041\vrcx-fork\src\        ← 人写的源代码（几百个小文件）
              │
              │  构建（npx vite build src，约 5 秒）
              ▼
C:\Users\28041\vrcx-fork\build\html\ ← 机器能跑的产物（十几个大文件）
              │
              │  部署（复制过去）
              ▼
E:\VRCX\html\                        ← VRCX 实际加载的位置
```

**这就是"改了代码要看效果，必须跑一次 sync"的原因。** 源代码改了不构建，界面永远是旧的。

一个判断技巧：`build/html/assets/` 里的文件名带着一串字母（比如 `plugins-BSoXWQeM.js`），这串是内容的"指纹"。内容一变指纹就变。所以如果你怀疑"我改的东西到底生效没有"，看这个文件名变没变就知道。

### 1.3 版本必须配对

VRCX.exe（宿主）和 html（前端）之间有一套私有通话方式，比如前端会喊 `AppApi.GetCurrentUser()`、`SQLite.Execute(查询语句)`。

- 官方每个版本，这两边是**一起发布**的，所以天然匹配
- 如果你拿**旧前端**装到**新宿主**上，可能前端喊了一个已经被删掉的方法 → 界面某块空白或报错
- 反过来，新前端装旧宿主也可能缺方法

这就是 `sync.ps1` 里那道"版本护栏"的意义：它会比较 `C:\Users\28041\vrcx-fork\Version`（你的分支对应的官方版本）和 `E:\VRCX\Version`（实际装的版本），不一致就拒绝部署，宁可你去看提示也别装出一个说不清问题的状态。

所以正确顺序永远是：**先让 VRCX 自己更新完 → 再跑 update → 最后部署**。

---

## 第二部分：git 是什么，你的"分支"在哪

### 2.1 用记账本来理解

git 是一个**给代码用的记账系统**。三个核心概念：

**提交（commit）**＝ 一笔账。记录"在某时刻，哪些文件的哪几行从什么变成了什么"，以及一句说明。你现在的两个提交：

```
e5a9a76a  Leave empty bio transitions out of the bio history   ← 过滤接口噪声
53a45002  Add per user bio history to the user dialog          ← 简介历史功能
```

每个提交前面那串字母数字（`e5a9a76a`）是它的编号，由内容算出来的，全世界唯一。

**仓库（repository）**＝ 整本账，加上全部历史。就是你硬盘上的 `C:\Users\28041\vrcx-fork`。里面有个隐藏的 `.git` 文件夹（105MB），装着官方 VRCX 从诞生到现在的全部 4155 次提交。

**分支（branch）**＝ 账本上的一个"书签"，指向某一笔账。`master` 是官方自己的书签，`my-vrcx` 是你的书签。

```
                    ┌── 53a45002 ── e5a9a76a   ← my-vrcx（你的）
… ── e08062a6 ──────┤                            ← 官方 v2026.09.16 的位置
                    └── (官方以后新增的提交)      ← master（官方的）
```

关键点：**分支不是复制了一份代码**，只是一个书签。你的两个提交是"接"在官方历史后面的，所以官方更新时，git 能算出"哪些是你加的"。

### 2.2 rebase 到底在干什么

官方发布了新版（比如 2026.10.xx），意味着官方的书签往前挪了 30 个提交。你的书签还挂在老地方。

**rebase（改基）就是把你的两个提交"剪下来，重新贴到官方新版的末尾"。**

```
之前：  官方旧版 ── 你的改动1 ── 你的改动2
                                    （官方已经走到 官方新版）

之后：  官方旧版 ── …官方新30个提交… ── 官方新版 ── 你的改动1' ── 你的改动2'
```

新贴上去的提交编号会变（`53a45002` → 变成另一个），因为它的"父提交"变了。这是正常的。

**为什么会冲突：** 如果官方那 30 个提交里，有人改了 `UserDialogInfoTab.vue` 的第 150 行，而你也改了第 150 行，git 就不知道该用谁的，于是停下来问人。这就是"冲突"。它不是错误，是 git 诚实地说"这里我判断不了"。

好消息是：你只改了 31 个文件，冲突范围有限，而且冲突时 rebase 会**完整保留现场**，你可以退回（`rebase --abort`）就像什么都没发生。**你的原始提交在 rebase 前一直存在**（git 内部有个"备份指针"叫 reflog，30 天内都能捞回来），所以 rebase 搞不坏东西。

### 2.3 上游 / 镜像 / 为什么有两个远程

- **上游（upstream）**＝ 官方的仓库。你的改动是"长"在它上面的，所以叫上游。
- **远程（remote）**＝ 别人硬盘/服务器上的那个仓库的网络地址。

你这台机器直连 `github.com` 是被网络掐断的（TLS 握手失败），所以我把上游地址设成了 `ghfast.top` 这个镜像站——它是个只读缓存，专门转发 GitHub 的内容。

**信任提醒：** 走镜像意味着你信任那个镜像不篡改代码。所以我还留了 `upstream_github` 这个官方直连地址。哪天你有梯子，或者想确认代码干净，就用 `-Remote upstream_github` 拉一次，两边对比。日常用镜像只是为了能连上。

---

## 第三部分：日常怎么维护

### 场景 A：我想看看现在装的是哪个版本

双击 `C:\Users\28041\vrcx-mod\vrcx-check.cmd`。

它只读不写，任何时候跑都安全。会告诉你四行：

```
  已安装的 VRCX   : 2026.09.16      ← E:\VRCX 里实际的宿主版本
  上游最新发布    : 2026.09.16      ← 官方当前最新版
  你的分支落后    : 0 个提交        ← 你的代码基线和官方差多少
  当前生效的前端  : my-vrcx @ 2026-09-23 12:31:38
```

最后一行是判断"我的改版到底还在不在"的最快方法。如果它显示"未部署（当前是官方原版前端）"，说明官方更新覆盖了你，需要跑场景 B。

建议：**每次打开 VRCX 前，如果距离上次更新超过一个月，就跑一下这个。** 十秒钟。

### 场景 B：官方更新了，我的功能没了

这是最常见的情况。官方更新器会重装整个目录，你的 `html` 被覆盖回原版。

三步：

1. **让 VRCX 正常更新完**（它自己会下载 `VRCX_Setup.exe` 安装并重启）。先让它更新，不要中途干预。
2. **完全退出 VRCX**，包括右下角托盘图标（右键 → 退出）。文件被占用时无法替换。
3. **双击 `vrcx-update.cmd`**。

它会依次做四件事，每步都有输出：

```
[1/4] 拉取 upstream/master ...
      上游 abc1234 (VRCX 2026.10.xx)，比你的分支基点多 37 个提交
[2/4] rebase my-vrcx 到 upstream/master ...
      rebase 干净通过
[3/4] 体检：lint + 构建 ...
      lint 和构建都通过
[4/4] 部署 ...
      [OK] 已部署 my-vrcx @ ...
```

看到最后那行就可以开 VRCX 了。

**如果第 2 步停住说冲突了**，屏幕上会打印两条路。你可以：

- 自己按提示操作（需要一点基础）
- 或者输入 `git -C C:\Users\28041\vrcx-fork rebase --abort` 完全退回，先继续用旧版
- 或者直接来找我说"rebase 冲突了，帮我修"——这是最省事的

**想提前知道会不会冲突**：双击 `vrcx-update.cmd` 时在后面加 `-DryRun`，或者在终端跑

```
C:\Users\28041\vrcx-mod\vrcx-update.cmd -DryRun
```

它会临时建个分支试一遍，报告"不会冲突"或列出冲突文件，然后撤销，不碰你的东西。

### 场景 C：我自己改了代码，想看效果

1. 完全退出 VRCX（含托盘）
2. 双击 `vrcx-sync.cmd`

它先检查（VRCX 没在跑？版本对得上？），再构建（约 5 秒），再备份旧前端，再部署，最后验证产物里确实有功能代码。

只想构建不部署，或者用上次构建结果快速部署：`vrcx-sync.cmd -SkipBuild`。

### 场景 D：想加一个新功能

**分支模型（2026-09-23 起）**：所有功能汇总在一条 `my-vrcx` 分支上，**一个功能一个 commit**；`master` 永远保持官方原样，作为对照和 rebase 的基线。

> 我最早跟你说过"每个功能开一条分支"。改成现在这样，是因为你只有一个人用、功能之间也互不依赖，多分支反而每次部署都要合并，纯属给自己找麻烦。"随时扔掉某个功能"的能力没丢——它从"删分支"变成"删 commit"，效果一样，操作更少。

做法就是直接在 `my-vrcx` 上干活：

```
cd C:\Users\28041\vrcx-fork
git checkout my-vrcx
# ……改代码……
git add -A
git commit -m "写为什么改，而不是改了哪个文件"
..\vrcx-mod\vrcx-sync.cmd      部署看效果
..\vrcx-mod\vrcx-push.cmd      满意了就存档到 GitHub
```

以后不想要某个功能了，跟我说"把 XX 那个 commit 去掉"，用 `git rebase -i` 摘掉就行。

`config.ps1` 里的 `Branch` 已经指向 `my-vrcx`，所以 `check-update.ps1` 报的"你的分支落后几个提交"就是按这条线算的。

### 场景 E：搞坏了，想回到干净状态

按破坏程度从轻到重：

**只想要回官方原版界面** —— 双击 `vrcx-rollback.cmd -Stock`。它把 `E:\VRCX\html_backup_20260923`（第一次改造前留的官方原版）换回去。数据库、登录、设置都不受影响。

**想回到上一次的自己** —— `vrcx-rollback.cmd -List` 列出所有备份（每次部署前自动留一份，保留最近 3 份），然后 `vrcx-rollback.cmd -Name 'html_backup\html_20260923-123108'`。

**代码改乱了想重来** —— 在仓库里 `git rebase --abort` 或 `git reset --hard HEAD` 丢弃未提交的改动。**注意 `reset --hard` 会丢掉没提交的东西**，用之前先 `git status` 看一眼。

**git 历史本身搞坏了** —— `C:\Users\28041\vrcx-mod\patches\` 里有两个 `.patch` 文件，是你那两个提交的完整文本描述。重新克隆一份官方仓库，然后：

```
git am C:\Users\28041\vrcx-mod\patches\0001-*.patch C:\Users\28041\vrcx-mod\patches\0002-*.patch
```

就能把功能原样重放出来。这是最底层的保险，永远用不上最好，但它在。

### 场景 F：把改动存到 GitHub（备份和换电脑的前提）

双击 `vrcx-push.cmd`。它一次管两个仓库：

```
── 代码  (C:\Users\28041\vrcx-fork)
   有 3 处未保存改动：
      M src/services/database/feed.js
      …
   给这次保存写一句说明（回车取消，不提交）：▌
── 维护脚本  (C:\Users\28041\vrcx-mod)
   没有未保存改动
```

它会让你输入一句中文说明（意思是"这笔账记的是什么"），然后提交并推送。

**什么时候该按它？** 每当你觉得"这个改动挺重要，丢了可惜"的时候。每一个说明就是一个存档点，以后想退回随时能退。

只推代码：`vrcx-push.cmd -OnlyCode`；只推脚本：`vrcx-push.cmd -OnlyMod`。

推送**不需要开梯子**（走 SSH 22 端口，实测过）。只有拉官方最新代码时才可能要用镜像或代理。

**说明写什么？** 写"为什么"而不是"改了什么"。比如"过滤接口偶发返回的空简介"就比"改了 feed.js"有用得多——半年后你只会记得前者。

---

## 第四部分：出问题怎么查

### "双击 cmd 弹出一堆红字"

红字不等于出错。git 习惯把正常进度信息写到"错误输出"通道，PowerShell 会把那通道的东西渲染成红色。判断标准是**最后一行**：脚本成功时会打印绿色的 `[OK] …`，失败时打印红色的 `[x] …` 并停下。

### "提示 VRCX 正在运行"

真的在运行。VRCX 关窗口后默认缩到右下角托盘继续跑。去任务栏右下角找它的图标，右键 → 退出。找不到就在任务管理器里结束 `VRCX.exe`。

### "版本不一致，拒绝部署"

说明官方宿主更新了但你的分支还没跟上去。**去跑 `vrcx-update.cmd`**，不要用 `-Force` 硬闯——那可能装出一个界面某块空白、但没人知道为什么的状态，那种问题最难查。

### "构建失败"

构建是"源代码 → 产物"的翻译过程，失败说明代码本身有问题（多半是 rebase 后接口对不上）。报错里会指出是哪个文件第几行。把整段报错发给我就行。

### "功能没出现"

按顺序检查：

1. `vrcx-check.cmd` —— 看"当前生效的前端"是不是你的分支。不是 → 没部署成功。
2. 是的话，按 `Ctrl+Shift+R` 强制刷新（VRCX 窗口里），有时是缓存。
3. 还不行，看是不是数据本身没有：简介历史只对**好友**、且只记录**VRCX 开着时**看到的变更。库里 176 条记录过滤掉噪声后只剩 28 条，涉及 15 个玩家，最多的是 MIRAI_21（7 次）。找不到人试的话就找他。

### "我想确认数据库没被动过"

`C:\Users\28041\AppData\Roaming\VRCX\VRCX.sqlite3`。所有脚本都不写它，只读。想彻底放心，改之前复制一份即可（VRCX 关闭时复制）。

---

## 第五部分：名词对照表

| 词 | 说的是什么 |
| --- | --- |
| 源码 / 源代码 | 人写的代码，在 `vrcx-fork\src\`。浏览器跑不了，需要构建 |
| 构建 / build | 把源码翻译成能跑的产物的过程，工具叫 Vite，产物在 `build\html\` |
| 前端 | 界面部分（网页文件），对应 `E:\VRCX\html\` |
| 宿主 | 真正干活的 .NET 程序，`E:\VRCX\VRCX.exe` |
| 部署 / sync | 把构建产物复制到 VRCX 加载的位置 |
| 仓库 / repo | 装了 git 记账系统的代码目录 |
| 提交 / commit | 一笔账：谁在什么时候改了哪些行 |
| 分支 / branch | 指向某个提交的书签，用来隔开不同功能线 |
| 上游 / upstream | 官方的原始仓库 |
| 远程 / remote | 别人机器上那个仓库的网络地址 |
| rebase | 把你的提交剪下来重新贴到官方新版末尾 |
| 冲突 / conflict | 官方和你改了同一处，git 停下来请人判断 |
| 镜像 | 转发 GitHub 内容的缓存站，用来绕过网络阻断 |
| 数据库 / sqlite | 存历史数据的单个文件，你的记录都在这 |
| SSH 密钥 | 证明"是我在推代码"的一对文件，公钥给 GitHub，私钥留本地 |

---

## 第六部分：换电脑 / 重装系统怎么恢复

你的东西在 GitHub 私有仓库 `W4nN1ng/VRCX-sweetCandy` 上，分三个分支，各管一块：

| 分支 | 里面是什么 |
| --- | --- |
| `master` | 官方 VRCX 原版代码，一条线都没动，用来对照和 rebase |
| `my-vrcx` | 官方代码 + 你的全部功能（简介历史、好友足迹，一个功能一个 commit） |
| `vrcx-mod` | 维护脚本、这份指南、patch 备份。和代码完全独立，不会互相干扰 |

在新电脑上从零恢复：

```powershell
# 1) 装两个必需的工具（去官网下载）
#    Node.js  → https://nodejs.org
#    Git      → https://git-scm.com

# 2) 克隆你的功能分支
cd C:\Users\<你的用户名>
git clone -b my-vrcx git@github.com:W4nN1ng/VRCX-sweetCandy.git vrcx-fork

# 3) 把脚本和指南单独取出来
git clone -b vrcx-mod git@github.com:W4nN1ng/VRCX-sweetCandy.git vrcx-mod

# 4) 装依赖（Electron 二进制下不来，跳过它，我们不用 Electron 跑）
cd vrcx-fork
$env:ELECTRON_SKIP_BINARY_DOWNLOAD = 1
npm ci

# 5) 先装一个官方原版 VRCX 到任意目录，然后改 vrcx-mod\config.ps1 里的
#    Repo / InstallDir / Branch 三个路径，指向新电脑上的实际位置

# 6) 部署
cd ..\vrcx-mod
.\sync.ps1
```

**SSH 密钥要重新配一次。** 私钥没有上传到 GitHub（传上去就没意义了），所以新电脑得重新生成一把、再贴到 GitHub 账号里，做法和第一次完全一样：

```powershell
ssh-keygen -t ed25519 -C "wn2276519232@gmail.com" -f ~/.ssh/id_ed25519_github_vrcx
cat ~/.ssh/id_ed25519_github_vrcx.pub | clip    # 公钥进剪贴板，去 GitHub 贴
```

贴完还要在 `~/.ssh/config` 里加那四行主机别名（内容抄 `vrcx-mod` 分支里的 `ssh-config-snippet.txt`）。

**数据库不用恢复也建议单独备份。** `C:\Users\28041\AppData\Roaming\VRCX\VRCX.sqlite3` 里是你的全部历史记录，它不在 GitHub 上（那是个人隐私数据，不该推到代码仓库）。想留底就在 VRCX 关闭时复制这个文件到网盘。丢了也只是历史没了，功能本身不受影响。

---

## 第七部分：这套东西的边界

诚实说明，免得你踩空：

1. **官方大改架构时可能不适配。** 比如哪天 VRCX 从 CefSharp 换成别的浏览器内核、或者把前端整个重写，我的部署方式（替换 html 目录）可能就不成立了。那时需要重新设计方案，不是跑个脚本的事。
2. **`E:\VRCX\html` 是官方安装器管的目录。** 任何官方更新都会覆盖它。这不是 bug，是它的更新机制本来如此（下载 `VRCX_Setup.exe` 重装整个目录）。
3. **走 ghfast.top 镜像拉代码有信任前提。** 它若篡改，你拉到的就是被改过的。想彻底干净就用官方直连（需要梯子）核对。
4. **SSH 私钥没有设密码。** 好处是脚本能自动用；代价是谁拿到这台机器的这个文件，就能以你的身份推代码。想加密码：`ssh-keygen -p -f ~/.ssh/id_ed25519_github_vrcx`。
5. **简介历史只覆盖好友、只覆盖 VRCX 运行期间。** 软件关着时别人改简介，补不回来。
6. **接口噪声是官方行为，不是我的 bug。** VRChat 接口在刷新好友列表时偶发返回空简介，官方原版把它当真实变更存了下来（你库里 176 条有 81 条是这种）。我只是在读取时过滤掉了，没去改历史数据。
